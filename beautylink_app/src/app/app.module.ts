/* import { BrowserModule } from '@angular/platform-browser';
import { NgModule } from '@angular/core';
import { AppRoutingModule } from './app.routes'; // Adjust the import path as necessary
import { AppComponent } from './app.component';
import { LoginComponent } from './components/login/login.component'; // Import the LoginComponent
// Import other components...

@NgModule({
  imports: [
    AppComponent,
    LoginComponent,
    BrowserModule,
    AppRoutingModule, // Make sure AppRoutingModule is imported here
  ],
  providers: [],
})
export class AppModule {}
 */
